name: Dev-Build

on:
  push:
  schedule:
    - cron:  '21 21 * * *'
  workflow_dispatch:

jobs:
  build:
    uses: axonivy-market/github-workflows/.github/workflows/ci.yml@v3
    with:
      # temporary: disable script validation ... connector uses an invalid variable name!
      mvnArgs: '-Divy.script.validation.skip=true'
