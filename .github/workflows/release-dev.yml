name: Release-Build-Dev
run-name: Release-Build-Dev ${{ github.ref_name }}${{ inputs.dryRun && ' (dry run)' || '' }}

on:
  workflow_dispatch:
    inputs:
      dryRun:
        description: Dry run mode for release and cleanup
        type: boolean
        required: false
        default: true

permissions:
  contents: read
  actions: read
  packages: write

jobs:
  release:
    uses: axonivy-market/github-workflows/.github/workflows/release-dev.yml@feature/MARP-4636-trigger-release-dev-multi-branches
    with:
      dryRun: ${{ inputs.dryRun }}
      javaVersion: 25
    secrets: inherit
